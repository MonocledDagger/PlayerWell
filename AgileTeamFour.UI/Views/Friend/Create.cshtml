@model AgileTeamFour.UI.ViewModels.FriendVM

@{
    ViewData["Title"] = "Send Friend Request";
}
<div class="container shadow p-5 mb-5 ps-5 rounded">
    <h2> Send Friend Request</h2>

    <hr />
    <div class="row">
        <div class="col-md-4">
            <form asp-action="Create">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            
            

                <div class="form-group">
                    <label asp-for="Friend.ReceiverID" class="control-label fs-5"></label>
                    <select asp-for="Friend.ReceiverID"
                            class="form-control"
                            multiple
                            asp-items="@(new MultiSelectList(Model.Users, "UserID", "UserName"))">
                    </select>
                    <span asp-validation-for="Friend.ReceiverID" class="text-danger"></span>
                </div>
                <div class="form-group m-2 mt-3">
                    <input type="submit" value="Send Request" class="btn btn-playerwell" />
                </div>
            </form>
        </div>
    </div>

    <div class="m-2">
        <a class="btn btn-secondary btn-sm" asp-action="Index">Back to List</a>
    </div>
</div>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
